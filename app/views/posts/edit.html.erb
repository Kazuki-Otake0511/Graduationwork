<% content_for :title, t('posts.edit_title_page') %>
<h1><%= t('posts.edit_title') %></h1>
<%= render 'form', is_edit: true %>

<!-- 削除ボタン -->
<div class="mt-3">
  <%= link_to t('posts.form.delete'), post_path(@post), method: :delete, data: { confirm: t('application.posts.form.confirm_delete') }, class: 'btn btn-danger' %>
</div>
